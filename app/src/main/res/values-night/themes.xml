<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NotedApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColorLight</item>
        <item name="colorPrimaryVariant">@color/primaryColor</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColorLight</item>
        <item name="colorSecondaryVariant">@color/secondaryColor</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="colorOnBackground">@color/lightGrey</item>
        <item name="colorTertiary">@color/grey</item>
        <item name="colorAccent">@color/darkGrey</item>
        <item name="fontFamily">@font/nunito_sans</item>
    </style>

    <style name="Theme.NotedApp.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:actionMenuTextColor">@color/white</item>
    </style>

</resources>